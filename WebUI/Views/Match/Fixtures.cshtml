@model IEnumerable<Domain.Models.ViewModels.FixtureVm>

@{
    ViewBag.Title = "Fixtures";
    Layout = "~/Views/Shared/_admin.cshtml";
}

<h2>Fixtures</h2>
@Html.ActionLink("Download Games", "LoadFixture", "Fixture")
@Html.ActionLink("Download File", "Download")
@Html.ActionLink("Download Score", "Index", "Result")
@Html.ActionLink("Excel Fixture", "Index", "Fixture")

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MatchNo)
        </th>
    
        <th>
            @Html.DisplayNameFor(model => model.HomeTeam)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AwayTeam)
        </th>
      
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MatchNo)
        </td>
      
        <td>
            @Html.DisplayFor(modelItem => item.HomeTeam)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AwayTeam)
        </td>
     
        <td>
            @Html.ActionLink("Edit Odd", "AddOdds", new {  id=item.MatchNo }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
