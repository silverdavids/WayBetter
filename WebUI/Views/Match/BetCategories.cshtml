@model IEnumerable<Domain.Entities.Models.BetCategory>

@{
    ViewBag.Title = "BetCategories";
    Layout = "~/Views/Shared/_admin.cshtml";
}


   <div class="row-fluid">
                        <!-- block -->
                        <div class="block">
                            <div class="navbar navbar-inner block-header">
                                <div class="muted pull-left">BetCategories</div>
                            </div>
                            <div class="block-content collapse in">
       <div class="span12">
    <table class="table table-bordered">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Bet_type)
        </th>
      
        <th>
           Active
        </th>
    
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Bet_type)
        </td>
       
        <td>
            @Html.DisplayFor(modelItem => item.Status)
        </td>
      
    </tr>
}

</table>
</div>
                                </div>
                            </div>
       </div>
